{
  "name": "FoundationSubgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ vagdonic/FoundationSubgraph",
    "create-local": "graph create --node http://localhost:8020/ vagdonic/FoundationSubgraph",
    "remove-local": "graph remove --node http://localhost:8020/ vagdonic/FoundationSubgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 vagdonic/FoundationSubgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.32.0",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
